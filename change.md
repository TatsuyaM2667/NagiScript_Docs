# ドキュメント改修の記録（2026年8月）

`docs/` 配下（リファレンス・チュートリアル）に対して行われた未コミットの変更をまとめます。方向性は、**旧設計草案に基づく記述を現行 v0.2 コンパイラーの実装に合わせて修正・削減**するというものです。「実際に動く機能」を基準に、空想・未実装の機能を明示的に区別する形へ改修しています。

> 注: この文書は docs/ の**変更内容そのもの**の記録です。ドキュメントの記述が実装と食い違う箇所もあるため、最後の「実装との整合に関する注記」も併せて確認してください。

---

## 目次

1. [全体の方針](#1-全体の方針)
2. [破壊的な構文の追随](#2-破壊的な構文の追随)
3. [未実装機能の一覧化](#3-未実装機能の一覧化)
4. [C 標準ライブラリ連携の見直し](#4-c-標準ライブラリ連携の見直し)
5. [CLI・設定ファイルの実装追従](#5-cli設定ファイルの実装追従)
6. [標準ライブラリ・型の実態に応じた整理](#6-標準ライブラリ型の実態に応じた整理)
7. [ファイルごとの変更内容](#7-ファイルごとの変更内容)
8. [実装との整合に関する注記](#8-実装との整合に関する注記)

---

## 1. 全体の方針

全ファイルに共通する改修方針は以下のとおりです。

- **現行 v0.2 実装に基づく旨を明記**: 各リファレンスの冒頭に「現行コンパイラーで動くものに基づく」という注記を追加。
- **未実装機能の明示**: 「まだ使えない機能」を列挙する節を新設し、実態に合わせて整理。
- **実行可能な例**: コード例を `fn main() { ... }` で包み、実際に動作する断片へ修正。
- **組み込み関数への統一**: `io.println` などを組み込みの `print()` / `println()` / `panic()` に統一。

---

## 2. 破壊的な構文の追随

実装に合わせて、以下の構文・型表記を全面的に修正しました。

- **関数の戻り値型**: `fn f() i32` → `fn f() -> i32` 形式へ統一。「暗黙 return は無く必ず `return` を明示」する旨を強調。
- **文字列型名**: `str` → **`string`** に全面変更。
  - `void` は「単型なし」、`type` エイリアスは「存在しない」と明記。
- **論理演算子**: `and` / `or` / `not` → **`&&` / `||` / `!`** に変更（`!` は従来どおり維持）。
- **ポインタ表記**:
  - アドレス取得を `&&` → 単一の **`&`** に修正（`unsafe` 内限定）。
  - ポインタ型を `*T` のみに（`*const` / `*mut` は無し）。
- **文字列 `+` 連結**: 「不可」と明記（`==` / `!=` は文字列に対して可）。
- **`?` 演算子**（エラー伝播）: 未実装と明記。
- **範囲演算子**: `..` / `..=` の説明を新設。

---

## 3. 未実装機能の一覧化

以下の機能を各ドキュメントで「未実装（ロードマップ）」と明記、または該当節を削除して未実装側へ整理しました。

- `import` / `export`（モジュール・モジュール分割）
- `async` / `await`
- `and` / `or` / `not` / `step` / `self`（キーワードとしては非対応扱いに）
- `const`（`val` を使用）
- `Result` / `Option` / `?` 演算子
- ジェネリクス（関数・構造体）・単型化（モノモーフィゼーション）
- 関数式・高階関数・クロージャ
- タプル・型エイリアス・関数型
- `@doc` コメント
- リストの `[]` 索引（`get()` / `set()` メソッドを使用）
- プリプロセッサマクロ `@dev { ... }` セクション
- 借用チェッカー（`check_borrow` 無しの方針を明記）

---

## 4. C 標準ライブラリ連携の見直し

`standard-c-library.md` を大幅削減し、「実験的・未整備」として整理しました。

- 冒頭に警告を追加: `extern "C" fn` の**シグネチャはパースされるが、任意の C 関数のコード生成は未整備**（`unknown callee` になる）と明記。
- stdio / stdlib / string / math / time / ctype / stdint の膨大な C 関数宣言・ラッパー例をほぼ全て削除。
- `unsafe` とポインタ操作（`&` + `as`、`*ptr`）の実例（`examples/unsafe_demo.ngs`）は維持。
- 未実装項目のロードマップ（`*const` / `*mut`、可変長引数、自動リンク手順）を新設。

---

## 5. CLI・設定ファイルの実装追従

コンパイラー CLI に合わせて、ハンズオン系の記述を更新しました。

- **推奨インストール**を `npm` → `cargo install ngs_driver` に変更（npm は副案に降格）。
- プロジェクト設定ファイルを `project.toml` → **`ngs-project.toml`** に修正し、`template = "native"` を追加、`[build]` の `entry` / `output` を削除。
- `--template native|web|wasm`（および `server` / `os`）の説明を追加。
- CLI 一覧に `wasm` を追加、`dts` に対象の注記を追加、`ir` の説明を「NGS 中間表現」に修正。
- ビルドオプションとして `--target` / `--emit-ll` を維持、**`--opt` が存在しない**旨を反映。
- 誤ったエラーコード書式（`Error[E001]`）や最適化パスの記述を削除。

---

## 6. 標準ライブラリ・型の実態に応じた整理

`standard-library.md` と `types.md` を実装に合わせて整理しました。

- `std:io` / `std:math` / `std:fs` / `std:args` / `std:async` / `std:net` は**存在しない**と明記し、各モジュール節を全削除。
- 組み込み関数（`print` / `println` / `panic`）の表と、**文字列補間 `f"..."`** の解説を新設。
- `List` は実装済みメソッド（`List.new()` / `push()` / `get()` / `set()` / `len()`）のみに整理。
- `Rc` は `Rc.new()` / `.get()` を掲載しつつ、**既知のバグ（`Rc.new(42).get()` が `1` を返す）**を注意書きで明記。
- 型変換を「拡張 = 暗黙可」「縮小 = `as` 必須」に整理（`f64` → `i32` の縮小ケースを明確化）。
- タプル・ジェネリクス構造体・Option/Result・関数型・型エイリアスを「未実装の型」節へ移動。
- enum 生成を常に括弧付き（`Color.Red()`）に統一。

---

## 7. ファイルごとの変更内容

| ファイル                          | 変更                                                                                                                                       |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `index.md`                        | 「言語リファレンス」に `change-log.md`（変更履歴）へのリンクを新規追加                                                                     |
| `getting-started.md`              | 推奨インストールを cargo へ、`ngs-project.toml` 化、`--template` 説明、CLI 一覧修正、`@dev` セクション削除                                 |
| `reference/compiler.md`           | パイプライン図を実情化、TokenKind / AST を実ソースと一致、ジェネリクス未実装化、誤エラーコード書式・`--opt` 削除、CLI/build 節新設         |
| `reference/keywords.md`           | キーワード表を実トークンに改訂、`string` 型化、未実装語の節へ整理、組み込み関数 table 新設                                                 |
| `reference/operators.md`          | `&&`/`\|\|` 化、単一 `&` 化、文字列 `+` 不可、`?` 未実装、`[]` 索引を未実装へ、`..`/`..=` 新設、優先順位表修正                             |
| `reference/standard-c-library.md` | 大幅削減、C 連携を実験的と警告、extern 例をほぼ削除、unsafe 例維持                                                                         |
| `reference/standard-library.md`   | 大幅削減、モジュール非存在を明記、組み込み関数 + `f"..."` 新設、List/Rc のみに整理、Rc バグ注記、ロードマップ表                            |
| `reference/syntax.md`             | `-> 型` 統一、暗黙 return 無し強調、関数式/クロージャ/ジェネリクスを未実装化、`impl` ブロック整理、enum 括弧化、`&&`/`\|\|`/`&`/`..` 化    |
| `reference/types.md`              | `string` 化、タプル/ジェネリクス/Result 等を未実装へ、List/Rc 実態化、`*T` のみ、型変換の縮小・拡張整理                                    |
| `tutorial/basics.md`              | `string` 化、`+` 連結と `s.len()` 削除 → `f"..."` 案内、制御構文例を `fn main()` 化、`&&`/`\|\|`/`!` 化                                    |
| `tutorial/error-handling.md`      | Result/Option/`?` を未実装として解説を削除し、`enum` + `match` + `panic` 方式に全面書き換え                                                |
| `tutorial/functions.md`           | タイトルを「関数とモジュール」→「関数」に、`-> 型`/暗黙 return 無しを明記、関数式/クロージャ/モジュール等をロードマップ表へ、`f"..."` 新設 |

---

## 8. 実装との整合に関する注記

本改修は「現行実装に追従」を方針としていますが、**一部の記述が実際の実装と食い違っている**可能性があります。確認した限り:

- **`for ... step`**: docs では「`step` は未実装」と明記されていますが、**実際のパーサーには `step` 節が実装済み**で、[変更履歴](./change-log.md#1-for--step-構文の追加)にも `for ... step` の追加が記録されています。実装との乖離があるため、`step` の記述は再確認が必要です。
- 上記以外の「未実装」記述（Result / Option / ジェネリクス / クロージャ / タプルなど）は、概ね実際の実装と一致する傾向です。

ドキュメントを実装と完全に同期させる場合は、この注記を起点に各記述を検証してください。

---

## 関連ドキュメント

- [変更履歴（言語機能） →](./change-log.md)
- [構文リファレンス →](./syntax.md)
- [標準ライブラリ →](./standard-library.md)
- [型システム →](./types.md)
